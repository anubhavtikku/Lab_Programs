#include<iostream>
using namespace std;
#include<stdio.h>
#include<string.h>
int main()
{ char s[100],a[20],b[20],c[20];
  int i;
    cout<<"Enter string ";
    gets(s);
    cout<<"Enter word to be deleted ";
    gets(a);
    for(i=0;s[i]!='\0';i++)
    {

        if(strcmp(b,a)==0)
        { strcpy(s,c);
          }
    }
    cout<<s;

return 0;
}
